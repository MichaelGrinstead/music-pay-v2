/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { hostname: "66ip4byohcwzudni.public.blob.vercel-storage.com" },
    ],
  },
};

export default nextConfig;
